
# План: Улучшение визуального оформления текста подписки

## Проблема
Текст о квесте в диалоге оплаты выглядит как сплошной блок — сложно читать. Нужно добавить визуальные разделители (пробелы, абзацы, эмодзи) для лёгкости восприятия.

---

## Решение

### Вариант реализации
Изменить формат `nextChargeInfo` в `BusinessTraining.tsx` — разбить на строки с эмодзи, а в `PaymentDialog.tsx` добавить поддержку переноса строк через CSS `whitespace-pre-line`.

### Новый текст (с эмодзи и разбивкой)

```
🎯 Это пошаговый квест с ежемесячным автосписанием.

📍 Присоединиться можно только в начале — войти с середины нельзя.

📚 Вы начинаете с 1-го модуля. Каждый оплаченный месяц = новый модуль.

✨ Отменить подписку можно в любой момент в личном кабинете — без звонков и обязательств.
```

---

## Изменения в файлах

### 1. `src/pages/BusinessTraining.tsx` (строки 504)

**Было:**
```tsx
nextChargeInfo: "Это пошаговый квест с ежемесячным автосписанием. Присоединиться можно только в начале — присоединяться с середины нельзя. Если вы покупаете сейчас, вы начинаете с 1-го модуля. Каждый оплаченный месяц = новый модуль. Вы можете отменить подписку в любой момент в личном кабинете — без звонков и обязательств.",
```

**Станет:**
```tsx
nextChargeInfo: "🎯 Это пошаговый квест с ежемесячным автосписанием.\n\n📍 Присоединиться можно только в начале — войти с середины нельзя.\n\n📚 Вы начинаете с 1-го модуля. Каждый оплаченный месяц = новый модуль.\n\n✨ Отменить подписку можно в любой момент в личном кабинете — без звонков и обязательств.",
```

### 2. `src/components/payment/PaymentDialog.tsx` (строка 1049)

Добавить CSS-класс `whitespace-pre-line` для корректного отображения переносов строк:

**Было:**
```tsx
<p className="text-muted-foreground">
  {subscriptionMessage.nextChargeInfo || "Следующее автоматическое списание произойдёт через месяц."}
</p>
```

**Станет:**
```tsx
<p className="text-muted-foreground whitespace-pre-line">
  {subscriptionMessage.nextChargeInfo || "Следующее автоматическое списание произойдёт через месяц."}
</p>
```

---

## Визуальный результат

```
┌────────────────────────────────────────────────────────┐
│  Квест «Бухгалтерия как бизнес»                        │
│                                                        │
│  Сегодня вы оплачиваете первый месяц обучения (250 BYN)│
│                                                        │
│  Это даёт вам мгновенный доступ к материалам после     │
│  старта 5 февраля 2026.                                │
│                                                        │
│  🎯 Это пошаговый квест с ежемесячным автосписанием.   │
│                                                        │
│  📍 Присоединиться можно только в начале — войти с     │
│     середины нельзя.                                   │
│                                                        │
│  📚 Вы начинаете с 1-го модуля. Каждый оплаченный      │
│     месяц = новый модуль.                              │
│                                                        │
│  ✨ Отменить подписку можно в любой момент в личном    │
│     кабинете — без звонков и обязательств.             │
│                                                        │
│  Управление подпиской доступно в вашем профиле 24/7.   │
└────────────────────────────────────────────────────────┘
```

---

## Файлы для изменения

| Файл | Строки | Что меняем |
|------|--------|------------|
| `src/pages/BusinessTraining.tsx` | 504 | Новый текст с `\n` и эмодзи |
| `src/components/payment/PaymentDialog.tsx` | 1049 | Добавить `whitespace-pre-line` |

---

## DoD

1. Текст разбит на абзацы с визуальными отступами
2. Эмодзи подчёркивают ключевые пункты
3. Легко читается на мобильных устройствах
4. Не перегружает диалог
