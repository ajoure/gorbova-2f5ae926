
# Редизайн блока статистики клуба: бизнес-метрики + симметричный iOS-glassmorphism

## Проблема

На скриншоте видно 3 проблемы:
1. **Карточки разной ширины** — сетка `grid-cols-6` растягивает карточки неравномерно в зависимости от длины контента
2. **Статистика не та** — показывает технические данные Telegram API, а не бизнес-метрики
3. **Нижняя панель** — информация дублируется и не несёт ценности

## Что хочет пользователь

Метрики клуба:
- **Сколько участников по тарифам** (BUSINESS: 34, CHAT: 11, FULL: 8 — есть в БД)
- **Сколько новых** (получили доступ за выбранный период — есть в `telegram_access_grants`)
- **Сколько не продлили** (доступ отозван/истёк за выбранный период — есть в `telegram_access_grants`)
- Возможность **перейти к фильтру** по клику на любую карточку

## Архитектура нового блока

### Данные (откуда брать)

```
Тариф BUSINESS: 34 чел. ← subscriptions_v2 JOIN tariffs JOIN product_club_mappings WHERE status IN (active/trial/past_due)
Тариф CHAT: 11 чел.    ← то же
Тариф FULL: 8 чел.     ← то же
Всего с доступом: 162  ← telegram_access_grants WHERE status=active AND end_at > NOW()
Новые (30д): 163       ← telegram_access_grants WHERE created_at >= NOW()-30d AND status=active
Не продлили (30д): 143 ← telegram_access_grants WHERE status IN (revoked,expired) AND updated_at >= NOW()-30d
Вне системы: 4         ← members_count_chat - stats.in_chat (уже есть)
```

### Новый хук `useClubBusinessStats(clubId)`

Добавить в `useTelegramIntegration.tsx` хук, который делает 2 запроса:
1. `supabase.from('subscriptions_v2').select(...)` — подписки по тарифам
2. `supabase.from('telegram_access_grants').select(...)` — новые / не продлили

Параметр `periodDays` (30 дней по умолчанию, можно выбрать 7/30/90).

### Новый компонент `ClubQuickStats.tsx` (полная замена)

#### Структура — 2 ряда равных карточек

**Ряд 1: Тарифы (динамическое число карточек)**
```
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   BUSINESS      │ │   CHAT          │ │   FULL          │ │ Всего с доступом│
│      34         │ │      11         │ │       8         │ │      162        │
│  участника      │ │  участника      │ │  участника      │ │                 │
└─────────────────┘ └─────────────────┘ └─────────────────┘ └─────────────────┘
```

**Ряд 2: Динамика (3 карточки фиксированных + 1 безопасность)**
```
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   Новые         │ │  Не продлили    │ │  Вне системы    │ │  Нарушители     │
│     163         │ │     143         │ │       4         │ │       0         │
│  за 30 дней     │ │  за 30 дней     │ │ (без аккаунта)  │ │   в чате/канале │
└─────────────────┘ └─────────────────┘ └─────────────────┘ └─────────────────┘
```

#### Симметрия карточек

Все карточки — **фиксированная минимальная высота `min-h-[100px]`**, `flex flex-col justify-between`. Сетка — `grid-cols-2 md:grid-cols-4` для каждого ряда. Тарифные карточки если их > 4, переносятся на следующую строку. Все карточки одинаковые по ширине (равные колонки грида).

#### Фильтр периода

Небольшой выпадающий список `7 дней | 30 дней | 90 дней` в правом верхнем углу панели. Меняет период для "Новых" и "Не продлили".

#### Кликабельность

- Карточка "Новые" — клик переходит на вкладку `with_access` (или можно добавить фильтр по дате, если реализован)
- Карточка "Не продлили" — переходит на вкладку `removed`
- Карточка "Нарушители" — переходит на вкладку `violators`
- Карточка тарифа — переходит на вкладку `with_access` (будущий фильтр по тарифу)

## Технические правки

### 1. `src/hooks/useTelegramIntegration.tsx` — добавить хук

```typescript
export function useClubBusinessStats(clubId: string | null, periodDays: number = 30) {
  return useQuery({
    queryKey: ['club-business-stats', clubId, periodDays],
    queryFn: async () => {
      if (!clubId) return null;
      
      const since = new Date(Date.now() - periodDays * 86400_000).toISOString();
      
      // 1. Подписки по тарифам
      const { data: subs } = await supabase
        .from('subscriptions_v2')
        .select('tariff_id, tariffs!inner(name, product_id, product_club_mappings!inner(club_id))')
        .eq('tariffs.product_club_mappings.club_id', clubId)
        .in('status', ['active', 'trial', 'past_due']);
      
      // 2. Новые за период
      const { count: newCount } = await supabase
        .from('telegram_access_grants')
        .select('*', { count: 'exact', head: true })
        .eq('club_id', clubId)
        .eq('status', 'active')
        .gte('created_at', since);
      
      // 3. Не продлили за период
      const { count: revokedCount } = await supabase
        .from('telegram_access_grants')
        .select('*', { count: 'exact', head: true })
        .eq('club_id', clubId)
        .in('status', ['revoked', 'expired'])
        .gte('updated_at', since);
      
      // Группировка по тарифам
      const tariffMap = new Map<string, { name: string; count: number }>();
      // ... aggregation logic ...
      
      return { tariffs: [...tariffMap.values()], newCount, revokedCount };
    },
    enabled: !!clubId,
  });
}
```

### 2. `src/components/telegram/ClubQuickStats.tsx` — полная переработка

- Удалить блок 6 карточек с Telegram API / В системе / В системе / ...
- Добавить 2 ряда по 4 карточки
- Все `GlassCard` — одинаковая высота `min-h-[100px]`
- Тарифные карточки — иконка 📋, цвет `info`/`success`/`amber` по очереди
- "Новые" — зелёный (success), "Не продлили" — красный (danger)
- "Вне системы" — фиолетовый (ghost), "Нарушители" — красный (danger)
- Каждая карточка — `cursor-pointer` при наличии `onClick`
- Переключатель периода `7д / 30д / 90д` — мелкий `<select>` или 3 кнопки в правом углу

### 3. `src/pages/admin/TelegramClubMembers.tsx`

- Передать в `ClubQuickStats` коллбек `onTabChange` для навигации по вкладкам
- `<ClubQuickStats club={club} stats={stats} members={members} isLoading={isLoading} isError={isStatsError} onTabChange={setActiveTab} />`

## Что остаётся без изменений

- Таблица участников — не трогаем
- Табы (В клубе / С доступом / Не вошли / Нарушители / Удалённые)
- Поиск, кнопки Обновить / Экспорт
- Все диалоги массовых действий
- Drawer с деталями участника
- Collapsible с полной статистикой `ClubStatistics`

## DoD

- A) Все 8 карточек одинаковой ширины и высоты (симметрия)
- B) Ряд 1: тарифы (BUSINESS 34, CHAT 11, FULL 8) + Всего с доступом 162
- C) Ряд 2: Новые/Не продлили/Вне системы/Нарушители
- D) Переключатель периода 7д/30д/90д меняет Новые и Не продлили
- E) Клик по "Нарушители" → переход на вкладку Нарушители
- F) Клик по "Не продлили" → переход на вкладку Удалённые
- G) Все подписи на русском, нет английских слов
