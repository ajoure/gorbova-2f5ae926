
# Индикатор «IP BY» на странице «Редакция»

## Что будет сделано

На странице «Редакция» (`/admin/editorial`), в шапке между заголовком и кнопкой «Запустить парсинг», появится компактный значок **IP BY** со следующим поведением:

- **Зелёный** (пульсирующая точка) -- BY-egress включён, VPS-прокси отвечает на `/health` (статус `ok`)
- **Красный** -- BY-egress выключен, VPS не отвечает, или произошла ошибка
- **Серый** (при загрузке) -- идёт проверка
- При наведении -- tooltip с деталями: URL прокси, статус, задержка
- Кнопка «переподключить» (иконка `RefreshCw`) -- повторная проверка `/health` в один клик

## Техническая реализация

### 1. Новый компонент: `src/components/admin/ByEgressStatusBadge.tsx`

Компонент:
- При монтировании загружает конфиг из `integration_instances` (provider = `hosterby`, поля `egress_enabled`, `egress_base_url`)
- Если `egress_enabled = true` -- вызывает `hosterby-api` с action `by_egress_check_health` для проверки `/health`
- Отображает Badge с иконкой `Globe` + текст «IP BY»
- Цвет: зелёный/красный/серый в зависимости от результата
- Кнопка `RefreshCw` для повторной проверки
- Tooltip с деталями (URL, latency, ошибка)

```text
[ 🟢 IP BY  🔄 ]   -- зелёный, работает
[ 🔴 IP BY  🔄 ]   -- красный, ошибка
[ ⏳ IP BY  ... ]   -- серый, проверка
```

### 2. Встраивание в `AdminEditorial.tsx`

В шапке страницы (строка ~1026, между описанием и кнопкой) добавить `ByEgressStatusBadge`:

```text
<div className="flex items-center justify-between">
  <div>
    <h1>Редакция</h1>
    <p>Мониторинг новостей...</p>
  </div>
  <div className="flex items-center gap-3">
    <ByEgressStatusBadge />         <-- НОВОЕ
    <Button>Запустить парсинг</Button>
  </div>
</div>
```

### Затрагиваемые файлы

| Файл | Изменение |
|------|-----------|
| `src/components/admin/ByEgressStatusBadge.tsx` | **Новый файл** -- компонент индикатора |
| `src/pages/admin/AdminEditorial.tsx` | Импорт + вставка компонента в шапку (2 строки) |

Никакие другие файлы не затрагиваются. Логика проверки использует существующий action `by_egress_check_health` из `hosterby-api`.
